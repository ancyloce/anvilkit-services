# Build context filter for the DEVELOPMENT_ONLY image builds that use the
# parent root as context (deploy/dev/images.sh): only the inputs the
# Dockerfiles copy enter the build.
*
!contracts/go
!jobs/shared/access-sidecar
jobs/shared/access-sidecar/.git
**/*_test.go
**/.local
