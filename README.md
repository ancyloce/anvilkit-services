# AnvilKit Services

AnvilKit Services is the repository for the AnvilKit Agent platform: a self-hosted system that turns a marketing team's requirement into complete, editable static-component source, certifies and approves an exact release, publishes it to npm and browser/CSS targets, activates it, and keeps saved pages locked to the exact release they used. The platform also includes bounded multi-agent execution, knowledge retrieval (RAG, vectors, long-term memory) and a reviewed MCP tool marketplace.

The architecture documents under [`docs/architecture/`](docs/architecture/architecture.md) are the source of truth. This README summarizes them and records what the checkout actually contains; it does not restate the design. Repository working rules for people and development agents are in [`AGENTS.md`](AGENTS.md).

## Status at a glance

| Aspect | Current state |
| --- | --- |
| Architecture baseline | Architecture document edition **V4.0, revision 2** — document metadata only, never a service, database, directory or release suffix ([naming](docs/architecture/architecture.md#naming)) |
| Decision | Rebuild the owned application implementation completely with the fixed technology selections ([ADR-001](docs/architecture/architecture.md#adr-001)); existing code in this checkout is outside the new build closure |
| Design status | Detailed design: DD-01 through DD-10 and ADR-001 through ADR-004 are adopted design decisions, not implemented behavior |
| Implementation status | R0 contract baseline (P01–P02), a DEVELOPMENT_ONLY foundation (P03), the LocalCheck control chain across API, Control and Workflow (P04–P05) and shared budgets with single-use dispatch admission (P06, corrected 2026-09-15), the obligation inventory, guarded effects and recovery reconciliation (P07, repaired 2026-09-16) and scoped artifact transfer with immutable stage acceptance (P08, 2026-09-16) exist and are locally verified; the fixed component certification chain (P10, 2026-09-16: `jobs/validator`, the `npm`/`browser`/`css` artifact classes and Control migration `00007`; its six review findings repaired in the twentieth pass — browser certification observed through Playwright outside the page, profile-mandatory checks, inventory-bound module loading, same-identity stop and confirmation, parsed CSS references, registry name and semver validation — with the image now running the browser check in the Pod; its SSR result-integrity and escaped-CSS review findings were repaired through the twenty-third and twenty-fifth passes (2026-09-17), the image rebuilt to `sha256:cc52643d…` at `validator-fixed-dev-v1` revision `6` with the admission policy realigned and applied to the cluster, and the four Go consumers bumped to the published contracts pseudo-version `v0.1.3-0.20260917133639-1e7cb5d5c537`) is implemented and verified on the host and through the launcher on the kind cluster with a DEVELOPMENT_ONLY Host ABI, and not complete while those consumer `go.mod` bumps stay uncommitted in their repositories; the contracts and the API live in their own repositories since 2026-09-15 and both are published (`anvilkit-agent-contracts` `main` `972e11c` — pushed; `go/v0.1.2` tagged at `532f680` on 2026-09-16, carrying `GetInstance` with its `operation` answer, `resultSizeBytes` and the `npm`/`browser`/`css` artifact classes beyond `go/v0.1.1` at `4025ff6`; `anvilkit-agent-api` `main` `83c0d9f`, which requires `go v0.1.2`, CI green); Control and Workflow were relocated into their own repositories on 2026-09-16 (sixteenth pass: `services/agent/{control,workflow}`, committed and pushed by the user as `c243540` / `9585920`, each with its `Dockerfile`, CI and chart, Control with its migrations and migration Job, both requiring the contracts pseudo-version of `a397fee` without a replace directive, verified alone and as two replicas each in the kind cluster; Control at `dd9c660` after the CI corrections and the runner-only S3 classification fix, CI green; the eighteenth pass's lifecycle and budget corrections — bounded Workflow shutdown, fatal-poller exit, Control PDB scope — are verified locally and uncommitted in those repositories); the parent's `main` `4dd7461` is pushed while `docs/`, `go.work`, `jobs/{codegen,migration}/` and `tests/` stay untracked ([Git inclusion](#git-inclusion)); the trusted Job boundary (P09, 2026-09-16: launcher templates, admission/syscall/network policies, the codegen harness and the access sidecar, verified locally; its gVisor candidate profile disabled) exists; the controlled Model Proxy (P11, twenty-first and twenty-second passes) exists; the bounded LangGraph/Pi coding team (P12, twenty-fourth pass, 2026-09-17: `jobs/codegen/team` with the ControlledModelPort over the sidecar relay, the one Pi source writer as the P09 candidate, the LangGraph TeamRunner with fixed roles and P06-style budgets, the joint StageStore over SqliteSaver with recovery against Control's accepted stage, the independent validation step through the validator's own chain with one bounded repair; the `stage` output class, the sidecar's accepted-stage read, the `anvilkit-codegen-team` image and the DISABLED profile `codegen-team-dev-v1`) is implemented and verified with real processes on the host against the development foundation, with a countable controlled upstream and no real model — its review findings repaired in the twenty-sixth and twenty-ninth passes (2026-09-17), the team image rebuilt from the repaired sources on the current validator and delivered to the development registry as `bbfdcf9c…` (`codegen-team-dev-v1` revision 2 in the contracts working copy, not yet published; [twenty-ninth-pass record](docs/architecture/delivery.md#verification-twenty-ninth-pass)); P13–P24 not started ([unit status records](docs/architecture/delivery.md#unit-status-records)) |
| Runtime qualification | All fourteen runtime gates G-01 through G-14 are `NOT_RUN`; the release qualification record is `NOT_VERIFIED` ([acceptance](docs/architecture/delivery.md#acceptance), [qualification lock](docs/architecture/platform.md#deployment-inputs)) |
| Pending inputs | Environment inputs ENV-01 through ENV-10 (machines, failure domains, prices/caps, IdP/Pagix/Studio contracts, RPO/RTO, on-call) are required before release and do not reopen technology selection ([deployment inputs](docs/architecture/platform.md#deployment-inputs)) |
| Initial versions | REST/SSE and Protobuf protocols begin at **v1**; the first stable application release target is **1.0.0** |

Adopted design decisions, observed implementation state, pending inputs and runtime qualification evidence are four different things throughout this repository. Historical approvals and proofs under `docs/archive/` (ignored by Git, present only in the original working copy) belong to the previous architecture and do not qualify the replacement.

## First milestone

The first milestone delivers one static component through the complete business chain. Each step is a separate lifecycle with its own identity, resources and clock ([lifecycles](docs/architecture/architecture.md#diagram-lifecycles)):

1. **Prepare** — a prompt becomes bounded structured requirements; at most two grouped clarification rounds of at most three questions each; selected sources, brand and assets freeze into a brief with content digests (M1-15 to M1-17).
2. **Generate** — a bounded agent team produces complete, editable component source under one shared operation budget; Pi is the only source writer (M1-02, M1-03, M1-11).
3. **Edit and preview** — conditional source save with expected revision; preview of exactly that revision in an isolated, separate-origin sandbox (M1-04).
4. **Certify** — an independent Validator with a trusted observer certifies npm, browser module, CSS and Puck/host compatibility; candidate reports never certify (M1-05).
5. **Approve** — a maintainer approves an exact `ReleaseSubject` binding source, version, artifact hashes, profiles and evidence; any changed field needs a new decision (M1-06).
6. **Publish and activate** — npm and browser/CSS publish through Pagix with both receipts verified before activation; a single `UNKNOWN` target keeps the release uncertain (M1-07, M1-08).
7. **Use in Studio** — new compatible components load without rebuilding the host; saved pages keep the exact release lock and never silently upgrade (M1-09, M1-10).

Operations are tenant-scoped, observable, cancellable and recoverable under original identities across replicas and failures (M1-01, M1-12 to M1-14). The seven capability requirements CAP-01 through CAP-07 (multi-agent, RAG/vectors, long-term memory, MCP marketplace, queue/events, Fx/configuration, Kubernetes) are part of the final scope; staging the work does not remove them. Details: [requirements](docs/architecture/requirements.md) and [user scenarios](docs/architecture/requirements.md#scenarios).

## Target architecture

The target is eight long-running application services on Kubernetes ([service catalog](docs/architecture/architecture.md#services)). All eight are **planned**; see [Repository state](#repository-state) for what exists.

| Service identity | Technology | Responsibility |
| --- | --- | --- |
| `anvilkit-agent-api` | Go, Gin | Authentication, OpenAPI validation, command mapping, authorized queries, SSE; no business database or provider key |
| `anvilkit-agent-control` | Go, gRPC, Fx | Admission, cost, execution and effect registries, artifacts, projections, Temporal relay; sole writer of the Control database |
| `anvilkit-agent-workflow` | Go, Temporal SDK | Fixed workflows (Preparation, Generation, PreviewBuild, Release, LocalCheck), bounded Runner, Activities, launch adapters |
| `anvilkit-agent-model-proxy` | TypeScript, pi-ai | Controlled provider adapter, stream normalization, usage capture; sole runtime holder of model provider keys |
| `anvilkit-agent-knowledge` | TypeScript, LangChain, native Qdrant client | Sources and ACL, ingestion, snapshots, retrieval, `MemoryFact`, projections, background tasks |
| `anvilkit-agent-mcp` | Go, official MCP SDK, Casbin | Reviewed catalog, grants, tool effects, protocol and SSRF policy |
| `anvilkit-agent-background-worker` | TypeScript, BullMQ | Claims durable Knowledge/MCP requests, runs bounded handlers, submits results; no domain database |
| `anvilkit-agent-inference` | Python, FastAPI, FlagEmbedding | Fixed embedding and reranking models with bounded batching; no business database or Qdrant access |

Codegen, Validator, Preview, Docling parser and migration are short-lived Job classes, not services. Temporal, PostgreSQL (CloudNativePG), Qdrant, Valkey, NATS JetStream, Apollo, OpenBao, RKE2/Cilium/Envoy Gateway and Rook-Ceph are infrastructure. Pagix keeps commercial, source, review, publication and activation authority; Studio keeps the page host and page writes; both are reached only through their authenticated APIs.

Governing decisions the entry points must not contradict:

- **Complete replacement.** New v1 contracts, empty target databases with `00001_init.sql` migrations, new Temporal histories and newly generated clients. Shared names never authorize reuse of old handlers, DTOs, SQL or executors. Business records or unresolved external obligations move only when an actual inventory shows they must, through separately verified import and handover work ([delivery](docs/architecture/delivery.md#implementation)).
- **Fixed technology, conditional alternatives.** The [60-decision matrix](docs/architecture/technology.md#choices) fixes each primary and its alternative. Primary-product replicas, quorum and failover provide high availability; an alternative is a planned replacement requiring migration and requalification, never an automatic heterogeneous standby.
- **Single authorities.** Temporal owns business progression; LangGraph coordinates bounded collaboration inside one execution attempt; Pi is the sole source-code writer; Control owns admission, costs, external effects and result acceptance ([ADR-002](docs/architecture/architecture.md#adr-002)). Knowledge owns source authorization and `MemoryFact`; MCP owns the reviewed catalog and explicit grants with the Control revocation barrier. Vector indexes, PostgresStore, caches and queues are rebuildable projections, never authorization or business-fact authorities.
- **Untrusted content and single-use sends.** Candidate code and retrieved or tool-returned content are untrusted; provider keys live only in Model Proxy and job scope credentials only in the trusted sidecar; validation is independent; every physical model or tool send needs its own single-use Control admission; `UNKNOWN` outcomes are reconciled under their original identity, never resent under a new one ([security](docs/architecture/security.md), [ADR-003](docs/architecture/architecture.md#adr-003)).

<a id="repository-state"></a>

## Repository state

Verified against the checkout on 2026-09-16 (eighteenth pass). Inspect the tree before relying on this table; it records observed state, not the target. Repository ownership (parent, contracts repository, service repositories) is defined in [architecture](docs/architecture/architecture.md#repositories).

<a id="git-inclusion"></a>

**Git inclusion (observed 2026-09-16, eighteenth pass, after `git fetch` of every remote; `git status --short` is the authority).** Observed again after the contracts tag and the bumps (`go/v0.1.2` at `532f680`, 2026-09-16): the parent's local `main` is `5e0c591`, five commits ahead of its pushed `4dd7461`, recording the gitlinks contracts `972e11c`, Control `1fbad8f`, API `83c0d9f`, Workflow `575fd38`, sidecar `402d423` and `jobs/validator` `d3760b9` — every one equal to its repository's pushed `main` — and tracking `go.work`, `go.work.sum` and `compose.yaml` since `aa6f0da`/`5e0c591`; still untracked: `docs/`, `jobs/codegen/`, `jobs/migration/`, `tests/`, `deploy/local/`; the concurrent P10 session's edits (`AGENTS.md`, `deploy/dev/`, `deploy/policies/`, `tools/`, `jobs/validator`, its entries in `delivery.md`) and this pass's `README.md`/`delivery.md` are uncommitted ([bump addendum](docs/architecture/delivery.md#verification-eighteenth-pass)). *(Earlier in the eighteenth pass.)* Every repository equals its remote: the parent's `main` `4dd7461` is pushed (the root documents, `tools/`, `deploy/dev/`, `deploy/policies/`, `pnpm-workspace.yaml` and the gitlinks: contracts `a6c2446`, API `f0b1145`, Control `dd9c660`, Workflow `9585920`, sidecar `b5e4d35`, `jobs/validator` `d3760b9` and the placeholder services); the service repositories are published with green CI at those commits. Still untracked in the parent and part of the reproducible composition: `docs/README.md`, `docs/architecture/` (11 documents), `go.work`, `go.work.sum`, `jobs/codegen/`, `jobs/migration/`, `tests/contracts/`, `tests/integration/`; untracked and deliberately left out (the never-tracked legacy environment definition, retained on disk until P24): `compose.yaml`, `deploy/local/`. Uncommitted from the eighteenth pass ([record](docs/architecture/delivery.md#verification-eighteenth-pass)): in `services/agent/workflow` the bounded shutdown and fatal-poller exit (`internal/bootstrap/{module,health}.go`, the lifecycle tests, `internal/config/config.go`, `config.yaml`, the chart's `shutdown.drainSeconds`, `README.md`); in `services/agent/control` the PodDisruptionBudget scope (`deploy/chart/templates/{deployment,pdb}.yaml`, `README.md`); in the parent this `README.md` and `docs/architecture/delivery.md`. A concurrent session's uncommitted P10 work (observed 13:12–13:18: `contracts/` sources and regenerated consumers, Control's domain, transport and a `00007` migration, the API's Control client, the sidecar's scope, Workflow's `policyfixtures_test.go`, the parent's `deploy/dev/images.sh`, the Kyverno Job policy and validator fixtures, `jobs/validator`) shares those working copies and is not part of the eighteenth pass. The grouped commit, push and clone operations that remain are listed at the end of that record; until the parent commits its untracked inputs and pins the two updated gitlinks, a recursive clone of the parent does not reproduce this checkout. The earlier observations that follow are kept as history and are superseded where they differ. *(Sixteenth pass.)* Control and Workflow now live in their repositories at `services/agent/{control,workflow}`: every legacy file was deleted in the worktrees (`.git`, `LICENSE` and `.gitignore` kept) and the replacement placed, and the user committed and pushed the result on each repository's `main` (`c243540`, `9585920`) and recorded the gitlinks in the parent (`4a78324`; contracts `a397fee` in `d5af5bf`); Control's CI corrections are pushed as `b83b6f9` and its runner-only S3 classification fix is still uncommitted in that repository; the source directories `services/anvilkit-agent-{control,workflow}` and the Control SQL under `jobs/migration` are removed; the concrete commit, tag and push operations that remain are listed in the [sixteenth-pass record](docs/architecture/delivery.md#verification-sixteenth-pass). The state before this pass, still accurate for everything else: The two extracted repositories are published: `anvilkit-agent-contracts` `main` `7806460` (tags `go/v0.1.0` at `c377f79` and `go/v0.1.1` at `4025ff6`; `7806460` is a README-only change over the tag; CI green) and `anvilkit-agent-api` `main` `f0b1145` (the requirement bump to `go v0.1.1` and the formatting rewrite; CI green, after the red run of `369ceb9`, which still required `v0.1.0`). The parent's local `main` (`e81c527`, eleven commits ahead of `origin/main` `85071e2`, **not pushed**) records the contracts gitlink `7806460` (`e81c527`) and the API gitlink `f0b1145` (`45b75be`), both current, the root documents (`1bf2c22`, `2e84f4b`, `fba5bf7`), `tools/`, `pnpm-workspace.yaml` and `deploy/dev/` (`df9a18c`). Still untracked at this pass and part of the export set: `docs/README.md`, `docs/architecture/`, `go.work`, `go.work.sum`, `jobs/migration/`, `services/anvilkit-agent-{control,workflow}/`, `tests/` and the retained legacy tooling `compose.yaml`, `deploy/local/`; modified and uncommitted: `.gitignore`, `deploy/dev/{README.md,compose.yaml,up.sh}` (the MinIO artifact store of P08), `tools/requirements.txt` and the legacy gitlinks of `services/agent/{control,workflow}` (user changes outside this extraction); untracked in addition: `deploy/dev/minio-setup.sh`. Until the remaining inputs are committed and the parent is pushed, a clean recursive clone of the parent does not reproduce this checkout; the [cleanup record](docs/architecture/delivery.md#cleanup-record) lists the proposed commit and the [tenth-pass verification](docs/architecture/delivery.md#verification-tenth-pass) what was verified from the source export instead.

| Path | Observed content | Relation to the V4.0 target |
| --- | --- | --- |
| `docs/architecture/` | Eleven architecture documents plus [`docs/README.md`](docs/README.md); not ignored (only `docs/archive/` is) and part of the export set, untracked until staged ([Git inclusion](#git-inclusion)) | Current authority |
| `docs/archive/` | Previous architecture (v3.12), its contracts (`docs/archive/contracts/`) and tools (`docs/archive/tools/`) | Historical reference only; its approvals and test results do not carry over |
| `contracts/` | The `anvilkit-agent-contracts` repository as a submodule (registered by parent commit `92e1edd`; its content committed and pushed on `main` — `0235295` is the content of `go/v0.1.0`, `f2cee94` adds the `verify.py` install-order fix, `4025ff6` of 2026-09-16 adds the P08 artifact-transfer and recovery-cursor contracts and is the content of `go/v0.1.1`, `7806460` names that version in the READMEs and is the commit the parent's gitlink records): `openapi/`, `proto/anvilkit/{control,knowledge,mcp}/v1` with `buf.yaml`, `jobs/`, `components/`, `events/` sources with fixtures; the generated consumers `go/` (module `github.com/ancyloce/anvilkit-agent-contracts/go`), `ts/` (`@anvilkit/generated-clients`), `python/`; `tests/go`; its own `tools/` and CI ([overview](contracts/README.md)) | Initial v1 sources and their generated consumers, one authority ([contract sources](docs/architecture/contracts.md#1-contract-sources-and-generated-bindings)); consumed as versioned packages: the Go module is published as tag `go/v0.1.1` (commit `4025ff6`; module hash `h1:w9BVVTxDkahXVUSwFrnDM1ZR+hbMGTFe/JkhATbGQhY=`, served by `proxy.golang.org` and recorded by the checksum database; `go/v0.1.0` at `c377f79`, hash `h1:2vk5EHKnTRoCpW5EHeMGWzfCp//M5MQ+EDWxRlNqXKc=`, is the previous version); the TypeScript/Python packages have no service consumer until P11/P15/P18/P20 |
| `services/agent/api` | The `anvilkit-agent-api` repository (submodule, history retained): the replacement API (Gin/oapi-codegen strict server + gin-contrib/sse, Fx, one typed `internal/config` and one secret-free `config.yaml`), its `Dockerfile`, CI and Helm chart `deploy/chart` ([overview](services/agent/api/README.md)); its `main` (`f0b1145`, pushed 2026-09-16: the API-12 artifact transfer of `369ceb9`, the formatting rewrite `9ca34a8` and the requirement bump; CI green) holds the replacement including the Fx entry point `cmd/anvilkit-agent-api/main.go`, `README.md` and `deploy/chart/`, requires `anvilkit-agent-contracts/go v0.1.1` with the published module hash and, from its repository alone, builds, tests and produces the image `sha256:bf1ba110…` from the public proxy (`46047a6`, the previous green commit, reproduced `sha256:ca2ac6a9…` the same way; `369ceb9` in between still required `v0.1.0` and had red CI); the parent's gitlink records `f0b1145` (local commit `45b75be`, [Git inclusion](#git-inclusion)) | Replacement implementation in its final location; builds from its repository alone against the published contracts module (public proxy, no replace directive, no workspace) |
| `services/agent/control` | The `anvilkit-agent-control` repository (submodule, history retained): the replacement Control (grpc-go, Fx, pgx/sqlc, goose, koanf, the S3 inventory and artifact adapters; one typed `internal/config`, one secret-free `config.yaml`), its migrations and migration Job (`internal/migrate/sql` at `00006`, `cmd/anvilkit-migration`), `tools/sqlc.sh`, `Dockerfile`, CI and Helm chart `deploy/chart` (Deployment, gRPC health probes, the migration Job as a hook) ([overview](services/agent/control/README.md)); placed on 2026-09-16 and pushed by the user as `main` `c243540`, CI corrections `b83b6f9` and the runner-only S3 classification fix `dd9c660` (CI green; the parent's gitlink); the eighteenth pass's PodDisruptionBudget scope (runtime Pods only, by the component label) was committed by the user as `09651a7`, `7ac4dc6` added P10's artifact classes and `00007` migration and `1fbad8f` (pushed 2026-09-16) the requirement bump — go, image and chart jobs green, the sqlc job failed once on a transient checksum-database read while installing sqlc; requires the published contracts module `go v0.1.2` (tag `go/v0.1.2` at `532f680`, public proxy, checksum database) without a replace directive | Replacement implementation in its final location; builds, tests and packages from its repository alone |
| `services/agent/workflow` | The `anvilkit-agent-workflow` repository (submodule, history retained): the replacement Workflow worker (Temporal SDK, Fx, koanf, client-go with kubeconfig or in-cluster identity, the health listener), `Dockerfile`, CI and Helm chart `deploy/chart` (Deployment, the launcher's ServiceAccount/Role/RoleBinding it owns) ([overview](services/agent/workflow/README.md)); placed on 2026-09-16 and pushed by the user as `main` `9585920` (CI green; the parent's gitlink); the eighteenth pass's bounded shutdown (one drain bound for both Task Queues, the SDK, Fx and the chart's derived grace period aligned), fatal-poller exit with withdrawn readiness and the bump to `go v0.1.2` are pushed as `6073239`…`575fd38` (2026-09-16; the parent's gitlink records `575fd38`); the same contracts requirement | Replacement implementation in its final location; builds, tests and packages from its repository alone; its cluster scenario consumes a Control the parent prepares |
| `jobs/migration` | goose/v3 migration Job with the `anvilkit_knowledge` and `anvilkit_mcp` schemas | Migration Job class of the domains without a service repository; `anvilkit_control` moved to Control |
| `tests/contracts`, `tests/integration` | The build-closure test; end-to-end LocalCheck and dispatch proofs on the dev foundation (`ANVILKIT_INTEGRATION_API_URL` runs the LocalCheck flows against an API deployed elsewhere) | Executable cases of the plan |
| `deploy/dev/` | Compose (PostgreSQL 17, Temporal 1.31.2, MinIO with the artifact and inventory buckets), kind cluster with the namespaces and the host-side launcher identity rendered from the Workflow chart, `up.sh`/`down.sh`; `values/anvilkit-agent-{api,control,workflow}.yaml` (parent-owned development values) and `{api,control,workflow}-chart.sh` (install each service's chart into the kind cluster) | DEVELOPMENT_ONLY foundation (P03); not HA, not RKE2 |
| `jobs/shared/access-sidecar` | The `anvilkit-job-access-sidecar` repository as a submodule: since P09 (2026-09-16) the trusted access sidecar (Go, UID 10002, `trusted.sock`/`candidate.sock`, Control scope lookup, scoped transfers and result submission), its `config.yaml`, `Dockerfile`, README and process tests; uncommitted in that repository ([overview](jobs/shared/access-sidecar/README.md)) | Replacement implementation of DD-03 §5 in its own repository; builds inside the workspace, and on its own once a contracts release with `ExecutionService.GetInstance` is published |
| `jobs/codegen`, `deploy/policies` | The trusted harness image of the codegen Job class (supervisor/observer/finalizer, privilege-drop trampoline, fixed candidate task) and the P09 admission (Kyverno 1.19.1), syscall and network policies with their offline/cluster check; since the twenty-fourth pass (2026-09-17) the team mode of the supervisor and `jobs/codegen/team` (`@anvilkit/codegen-team`, its own pnpm package outside `go.work`: the trusted coordinator — LangGraph.js 1.4.15 `StateGraph` with `@langchain/langgraph-checkpoint-sqlite`, the specialists over the sidecar's model relay, the joint StageStore, the validator's chain as the independent validation step — and the Pi coder on `@earendil-works/pi-coding-agent` 0.85.1 as the candidate), `agent/team/` (trusted prompts, the reviewed tools document), `team.yaml`, `fixtures/brief.json` and `Dockerfile.team` (the `anvilkit-codegen-team` image, the validator image plus the supervisor and the team package; [overview](jobs/codegen/team/README.md)) | P09 implementation; the gVisor profile `codegen-fixed-v1` stays disabled ([P09 record](docs/architecture/delivery.md#unit-status-records)); P12 implementation, its profile `codegen-team-dev-v1` DISABLED (model-written code, gVisor; the admission policy names no team Pod) and verified with real processes on the host only ([P12 record](docs/architecture/delivery.md#unit-status-records), [twenty-fourth-pass record](docs/architecture/delivery.md#verification-twenty-fourth-pass)) |
| `services/agent/model-proxy` | The `anvilkit-agent-model-proxy` repository as a submodule: since P11 (twenty-first pass, 2026-09-16) the controlled Model Proxy (TypeScript, `@earendil-works/pi-ai` 0.85.1 on the inspected `openai-completions` path with the SDK's fetch injection, `eventsource-parser`/`eventsource-encoder` for the event stream, Node.js 24, its own pnpm package outside `go.work`; `@anvilkit/generated-clients` as a git-hosted dependency pinned to contracts `972e11c` — behind the workspace's `Message.toolCalls` until bumped), its `config.yaml`, `Dockerfile`, CI, Helm chart (probes on a plaintext health port) and Vitest suite ([overview](services/agent/model-proxy/README.md)); uncommitted in its repository; the twenty-second pass (2026-09-17) repaired its P11 review findings | P11 implementation; the controlled route is enabled only by the verification scenarios, no real provider route exists |
| `services/agent/knowledge` | The `anvilkit-agent-knowledge` repository as a submodule: since P14 (thirty-first pass, 2026-09-17) the owner side of `anvilkit.knowledge.v1.BackgroundTaskService` (TypeScript, grpc-js, pg; one client transaction per command with the watermill-compatible outbox insert), the Go forwarder sidecar module `forwarder/` (watermill-sql subscriber → JetStream, least-privilege role), configuration generations with secret rotation, `config.yaml`, `Dockerfile` (service and `forwarder` targets), CI, Helm chart (service + forwarder + relay sidecars), Vitest suite; uncommitted in its repository ([overview](services/agent/knowledge/README.md)) | P14 implementation; sources, ingestion, retrieval and MemoryFact arrive with P15–P17 |
| `services/agent/mcp` | The `anvilkit-agent-mcp` repository as a submodule: since P14 the owner side of `anvilkit.mcp.v1.BackgroundTaskService` (Go, grpc-go, Fx, pgx/sqlc, koanf; the watermill-sql publisher bound to the same `pgx.Tx` as every domain write, the in-process forwarder, the lease sweeper with the Control dispatch query, generations with secret rotation), `sqlc.yaml`/`tools/sqlc.sh` (schema from `jobs/migration` until MCP owns its migrations), `config.yaml`, `Dockerfile`, CI, Helm chart (service + relay sidecar), Testcontainers suite; uncommitted in its repository ([overview](services/agent/mcp/README.md)) | P14 implementation; catalog, grants and tool calls arrive with P18/P19 |
| `services/agent/background-worker` | A plain directory of the parent (no repository exists yet): the Background Worker and the owner queue relay (TypeScript, BullMQ 6, `@nats-io` JetStream, Bull Board), `config.yaml`, `Dockerfile`, CI, Helm chart, Vitest suite with real PostgreSQL/Valkey/NATS ([overview](services/agent/background-worker/README.md)) | P14 implementation; the `anvilkit-agent-background-worker` repository is to be created and registered |
| `packages/profile-schemas` | The shared configuration schemas (`apollo-snapshot`, `config-generation`), their cross-language fixtures and the executable Python consumer example for the Inference service | P14-06; consumed by the Go (MCP) and TypeScript (Knowledge) loaders' tests and the `profiles` step of the chain |
| `deploy/local/`, `compose.yaml`, `tools/prepare-local-compose.py` | Docker Compose profile of the legacy local-check flow (PostgreSQL 18.6, Temporal 1.31.2), mounts repointed to `docs/archive/contracts/`; the legacy API, Control and Workflow services pinned to their own images (`legacy-20260915`, `legacy-20260916`) and the preserved legacy worktrees under `.local/legacy/` | Legacy environment; not handed over, not a startup procedure for the replacement |
| `packages/biome-config`, `packages/typescript-config`, `apps/` | pnpm/Turborepo starter | Starter only; the Model Proxy is its own pnpm workspace root (as `contracts/` and `jobs/validator`), Knowledge arrives with P15 |

Not present in any form: `anvilkit-agent-inference` (the Python configuration consumer example under `packages/profile-schemas/python` is its P14-06 input), the `jobs/{preview,parser}` classes and `deploy/gitops`; `anvilkit-agent-background-worker` exists as a directory without a repository.

## Reading route

Start with [`docs/README.md`](docs/README.md), then read in this order:

| Document | Read it for |
| --- | --- |
| [Architecture](docs/architecture/architecture.md) | Scope, the eight services and dependency direction, lifecycles, naming and initial versions, ADR-001 to ADR-004, terminology |
| [Requirements](docs/architecture/requirements.md) | Product profile, M1-01 to M1-17, CAP-01 to CAP-07, UI constraints, scenarios |
| [Technology](docs/architecture/technology.md) | The 60 primary/alternative choices, inherited version targets, licensing boundaries |
| [Contracts](docs/architecture/contracts.md) | Contract sources, public endpoints, internal method groups, values and errors, events/SSE, data ownership, lock ranks |
| [Execution](docs/architecture/execution.md) | DD-01 Workflow, DD-02 Control and single-use dispatch, DD-03 agent harness and isolation |
| [Components](docs/architecture/components.md) | DD-04 build and certification, DD-05 Studio, DD-06 Pagix ports |
| [Knowledge](docs/architecture/knowledge.md) | DD-07 ingestion, vectors, retrieval, memory facts |
| [MCP](docs/architecture/mcp.md) | DD-08 catalog, grants, revocation barrier, tool calls, SSRF |
| [Platform](docs/architecture/platform.md) | DD-09 queues/events/Fx/configuration, DD-10 Kubernetes/HA/recovery, ENV inputs, operations |
| [Security](docs/architecture/security.md) | Trust regions, SEC-01 to SEC-12, logging and deletion, supply chain |
| [Delivery](docs/architecture/delivery.md) | Stages R0–R6, definition of complete replacement, gates G-01 to G-14, traceability, evaluation |

## Commands

Checked on 2026-09-16 (sixteenth pass). None of them qualifies a G gate.

| Command | Effect | Prerequisites | Writes files |
| --- | --- | --- | --- |
| `sh tools/verification-env.sh [--static \| --only STEP]` | Creates or reuses `.local/verification-venv` (CPython 3.12, `tools/requirements.txt` fully pinned), installs the Python consumer package in place, then runs `tools/run-verification.py` with the given arguments | CPython 3.12 (`ANVILKIT_PYTHON` may name it), network for the first install | `.local/verification-venv/` only |
| `python3 tools/check-docs.py` | Current-baseline Markdown check (fences, tables, links, anchors); missing required inputs fail | Python 3 (standard library) | No |
| `python3 tools/check-contracts.py [--against REF]` | Delegates to `contracts/tools/check.py`: `buf lint`, OpenAPI validation, JSON Schema metaschema + fixtures, OpenAPI vectors of all three specs, RPC vector message names (`--against` names a ref of the contracts repository) | The pinned toolchain (`.local/verification-venv/bin/python`); any other interpreter exits 2 UNEXECUTED with the install command; buf 1.73.0 | No |
| `python3 tools/check-source-export.py [--keep DIR]` | Git export set (the parent's tracked + untracked-not-ignored files plus the contracts, API, Control, Workflow and sidecar repositories' own listings) holds every declared input and no archive/secret/excluded-submodule content; a disposable copy passes the docs/contract checks, builds every `go.work` module and builds the three service repositories with `GOWORK=off` on its own | The pinned toolchain, Go, buf | Temp dir only (or `--keep`) |
| `python3 tools/generate-contracts.py [--check] [--only contracts\|sqlc]` | Two separate generators: the contract bindings through `contracts/tools/generate.py` (Go, TypeScript, Python consumers of the contracts repository) and Control's sqlc output through `services/agent/control/tools/sqlc.sh` (its queries and its own migrations); `--check` diffs each against a scratch tree | The pinned toolchain; buf, protoc-gen-go, protoc-gen-go-grpc, oapi-codegen, sqlc in `GOPATH/bin`; `pnpm install --frozen-lockfile` done in `contracts/` — versions in `contracts/README.md` | Without `--check`: yes |
| `sh tools/verification-env.sh` in `contracts/` | The contracts repository's own chain (check, generate `--check`, go with `GOWORK=off`, ts, python), reading nothing outside that directory | CPython 3.12, Go, buf and the generators, Node.js 24 + pnpm | `contracts/.local/` only |
| `python3 tools/run-verification.py [--static]` | docs → generate `--check` → contracts → export → ts (`pnpm install --frozen-lockfile` in `contracts/`, tsc, Biome, Vitest) → python (pytest in `contracts/python`) → `go build/vet/test` per `go.work` module → integration (needs the dev foundation) | The pinned toolchain, Go 1.26+, Node.js 24 + pnpm (`packageManager`), Docker for Testcontainers unless `--static` | No (scratch only) |
| `sh deploy/dev/api-chart.sh [install\|uninstall]` | Builds the API image from `services/agent/api` alone, loads it into the kind cluster, creates the principals Secret from `.local/dev/api-principals.json` and installs the API's chart with the parent's development values (`deploy/dev/values/anvilkit-agent-api.yaml`) | The dev foundation, Docker, kind, kubectl, helm; the image build resolves the published contracts module through the public proxy (`ANVILKIT_DEV_GOPROXY`/`ANVILKIT_DEV_GONOSUMDB` only for a private proxy); a rebuilt image rolls the Pods through the `anvilkit.io/image-id` Pod annotation | The kind cluster only |
| `sh deploy/dev/control-chart.sh [install\|uninstall]`, `sh deploy/dev/workflow-chart.sh [install\|uninstall]` | Build each image from its repository alone, load it into the kind cluster, create Control's four Secrets from `.local/dev/` and install each repository's chart with the parent's development values (`deploy/dev/values/anvilkit-agent-{control,workflow}.yaml`): two Control replicas on the shared `anvilkit-inventory` bucket with the migration Job hook, two Workflow replicas with the chart-owned launcher identity | The dev foundation with `.local/dev/env.sh` sourced, Docker, kind, kubectl, helm; Control before Workflow; the in-cluster workers share the Temporal namespace with host-side test workers (uninstall before the integration scenarios) | The kind cluster only |
| `sh deploy/dev/up.sh` / `sh deploy/dev/down.sh` | DEVELOPMENT_ONLY foundation: PostgreSQL 17, Temporal, MinIO (artifact and inventory buckets), migrations (forward, in place; Control's through its repository's Job), kind cluster with the namespaces and the host-side launcher identity rendered from the Workflow chart; `env.sh` exports the placement overrides, the cluster-side addresses and each service's `ANVILKIT_*_CONFIG` path | Docker, kind, kubectl, helm, Go | `.local/dev/` only |
| `go test -tags integration ./...` in `tests/integration` | End-to-end LocalCheck on the dev foundation | `. .local/dev/env.sh` | Temp dirs only |
| `GOWORK=off GOFLAGS=-mod=readonly go build ./... && go vet ./... && go test ./...` in `services/agent/api` | Builds, vets and tests the API from its repository alone against the published contracts module (`go/v0.1.3` from `proxy.golang.org`, checksum-database verified, since the twenty-eighth pass's uncommitted bump — the committed `main` `e4bbf67` still names the pseudo-version `v0.1.3-0.20260917133639-1e7cb5d5c537`; a module cache that still holds the retired local-proxy artifact of `go/v0.1.0` fails with a checksum mismatch — evict that entry or use a fresh `GOMODCACHE`); `docker build .` and `helm lint deploy/chart` likewise | Go 1.27, Docker, helm | Go build cache only |
| `GOWORK=off GOFLAGS=-mod=readonly go build ./... && go vet ./... && go test ./...` in `services/agent/control` and `services/agent/workflow` | Builds, vets and tests each service from its repository alone against the published contracts module `go/v0.1.3` (commit `ffe05d5`, `proxy.golang.org`, checksum-database verified; verified with a fresh `GOMODCACHE` on 2026-09-17 for the API, Control, Workflow and the sidecar after the uncommitted bump from the pseudo-version of `1e7cb5d`); `sh tools/sqlc.sh --check` (Control), `docker build .` and `helm lint deploy/chart` likewise | Go 1.27, Docker (Testcontainers), sqlc 1.27.0, helm | Go build cache only |

## Known limitations for the next phase

- **Development-only components.** The filesystem inventory adapter in Control, the bearer-token fixture verifier in the API, plaintext loopback gRPC and the kind/Compose topology exist only for development; ENV-02/03/07 inputs replace them in P07 and P22/P23.
- **Gates.** G-01 to G-14 remain `NOT_RUN`; the passing tests recorded in [delivery.md](docs/architecture/delivery.md#unit-status-records) are unit verification of P01–P05.
- **P09 repairs and delivery.** F1/F2 node placement, scheduler and resource/volume constraints and F3 post-Control deadline checks are repaired. The rebuilt supervisor and sidecar are pinned at profile revision 2 and have run in the Docker-backed development cluster. Target gVisor/Cilium/PKI qualification and remote delivery remain UNEXECUTED; candidate profiles remain disabled. See the [current P09 acceptance record](docs/architecture/delivery.md#verification-p09-repair).
- **Not yet implemented.** Everything from P14 on; the API answers `DEPENDENCY_UNAVAILABLE` for those paths. P13 exists as the Preparation and Generation lifecycles ([twenty-eighth-pass record](docs/architecture/delivery.md#verification-twenty-eighth-pass)): API-01/API-06, the `PreparationWorkflow` (analysis through the controlled Model Proxy, at most two grouped rounds of three questions with a seven-day absolute wait each, answers as tracked Updates, the frozen brief artifact) and the `GenerationWorkflow` (execution permit that sets the active deadline once, scope/funding/lease bootstrap under stable occurrences, codegen and independent validator Jobs launched with their inputs by handle, bounded repair on accepted repairable findings, candidate registration under an effect identity, lease supervision, hold/resume/definition changes as tracked Updates), verified on the host against real PostgreSQL/Temporal/MinIO, a countable controlled upstream and DEVELOPMENT_ONLY Pagix/Knowledge doubles; no Generation reaches `candidate_ready` here because its codegen profile (`codegen-team-dev-v1`) stays DISABLED (gVisor), the contracts it adds are published as `go/v0.1.3` (`ffe05d5`) and the four Go consumers are bumped to it (standalone `GOWORK=off` builds and tests pass; the bumps are uncommitted in their repositories), while the sidecar, supervisor and validator images are not rebuilt from this pass's sources. P12 exists as the bounded LangGraph/Pi coding team inside the codegen Job (`jobs/codegen/team`; [twenty-fourth-pass record](docs/architecture/delivery.md#verification-twenty-fourth-pass)): verified on the host with a countable controlled upstream only, its profile DISABLED, no Knowledge retrieval exists behind its Retrieval role (P16: the role answers insufficient evidence); its cross-attempt resume and the validator-Job handoff of a team source are implemented by P13 (sidecar loads by handle, `ReadArtifact`) but not yet exercised in a Pod. P10 exists as the fixed component certification chain of `jobs/validator` (a reviewed fixed Hero component, the protected Rollup build loading only inventoried modules, the independent Validator whose browser verdict comes from Playwright's observation outside the page — isolated-world DOM/CSSOM, request log, real input, a re-render with the observer's own value — with every profile-mandatory check required for `certified`, and the `validator-fixed-dev-v1` Job, revision `6` (image `sha256:cc52643d…` since the twenty-fifth pass's SSR result-integrity and escaped-CSS repairs), whose image runs the SSR and browser checks in the Pod and whose npm/browser/css/evidence artifacts Control accepts) verified with real processes on the host and through the launcher on the kind cluster; its Host ABI is DEVELOPMENT_ONLY (no Studio evidence, ENV-08), its Job runs reviewed content only, qualifying no candidate isolation, and the contracts profile is committed and pushed (`1e7cb5d`) with the four Go consumers bumped to its published pseudo-version `v0.1.3-0.20260917133639-1e7cb5d5c537` (which also clears the standalone Workflow `GOWORK=off` build), but those consumer `go.mod` bumps stay uncommitted in their repositories, so a clean recursive clone does not yet reproduce them ([P10 record](docs/architecture/delivery.md#unit-status-records), [twentieth-pass record](docs/architecture/delivery.md#verification-twentieth-pass)). The trusted Job boundary (P09) exists and is verified with real processes on the host and with the development wiring profile on the kind cluster, but its candidate profile `codegen-fixed-v1` is disabled everywhere: the gVisor/CNI/kernel combination it requires is not available on the development foundation (G-04/G-05 `NOT_RUN`; [P09 record](docs/architecture/delivery.md#unit-status-records)). External effects, the obligation inventory and recovery (P07) and scoped artifact transfer with accepted results (P08) exist against development backends: the filesystem inventory, a MinIO artifact store (`deploy/dev`) and DEVELOPMENT_ONLY outcome-query, evidence and operator fixtures; the RGW inventory and the production object backend (ENV-02), real Pagix/Model Proxy queries (ENV-07, P11) and production operator authority remain deployment inputs. Result manifest outputs of the parser class (`chunks`) cannot yet be bound to artifacts; P15 defines that binding (the `npm`/`browser`/`css` classes bind since P10, the `stage` class since P12). Budgets and single-use dispatch (P06) exist with DEVELOPMENT_ONLY prices, route authorizations and not-sent evidence (ENV-06/ENV-07); no paid route is enabled. An operation whose Job cleanup could not be confirmed is closed `reconciling` with its cancel `pending` while its own LocalCheck run keeps reconciling the original launch key until the backend evidence settles it; only past the run's configured bound (`execution.cleanup.reconcile_max_duration`, default 24 h) does the run fail visibly and leave the record for the P07 recovery epoch and operator dispositions. Accepted results keep their original bytes (`stage_manifests.result_manifest_bytes`, verified on read); stages accepted before migration `00002` have no original bytes and are never backfilled. The generated TypeScript/Python consumers have no service consumer yet (P11/P15/P18/P20). Apollo generations, reload and secret rotation arrive with P14 on top of the per-service configuration files.
- **Git baseline.** The contracts are published: `anvilkit-agent-contracts` `main` `7806460` holds the sources, consumers, fixtures and tools (its CI is green), the tag `go/v0.1.1` (commit `4025ff6`, 2026-09-16) is the current Go module version and the recorded `buf breaking` baseline (`contracts/tools/check.py --against go/v0.1.1`; the previous baseline `go/v0.1.0` at `c377f79` also passes, the change is additive) and both module versions are served by the public proxy and recorded by the checksum database; the redundant draft PR #1 and its branch `publish/v1-contract-baseline` are closed and deleted (only the local branch `feat-contracts` and the local lightweight tags `v0.1.0` and `v0.1.1` in `contracts/`, at or below the published commits, remain as the user's leftovers). The API is published: `anvilkit-agent-api` `main` `f0b1145` (requires `go v0.1.1`, CI green); the contracts tag `go/v0.1.2` (commit `532f680`) exists since 2026-09-16 and is served by the public proxy and recorded by the checksum database; the four consumers require it on their pushed `main` — Control `1fbad8f`, the API `83c0d9f` (CI green), Workflow `575fd38` (with the eighteenth pass's lifecycle change), the sidecar `402d423` — each verified from its repository alone with a fresh module cache before the commits. The parent's local `main` `5e0c591` (five commits ahead of its pushed `4dd7461`) records the gitlinks contracts `972e11c`, Control `1fbad8f`, API `83c0d9f`, Workflow `575fd38`, sidecar `402d423` and now tracks `go.work`, `go.work.sum` and `compose.yaml`, but `docs/`, `go.work`, `go.work.sum`, `jobs/{codegen,migration}/` and `tests/` are still untracked and the eighteenth pass's Control and Workflow changes are uncommitted in their repositories ([Git inclusion](#git-inclusion)); the recursive parent-clone acceptance and the registry publication of the images therefore have not run. Retired with the publication: the local module proxy `.local/goproxy/` (a copy stays in the [pre-publication snapshot](docs/architecture/delivery.md#source-snapshot)); its stale artifact in the machine's module cache was evicted in the tenth pass. A source snapshot is never a substitute for a commit.
